const x = "12";
const y = "3";
console.log(Boolean(x));               //If the value is stored then the boolean value will show the true
console.log(Boolean(""));              //If the value is not stored then the boolean value will show the false.
console.log(Number(x) + Number(y));    //Converting String into numbers.

const a = 12;
const b = 3;
console.log(String(a) + String(b));    //Converting Numbers into String.